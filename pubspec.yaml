name: mini_chat_application
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  dio: ^5.9.0
  retrofit: ^4.5.0
  flutter_bloc: ^9.1.1

  bloc: ^9.2.0
  equatable: ^2.0.8
  loader_overlay:
  http: ^1.6.0
  another_flushbar: ^1.12.32

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

  build_runner: ^2.4.13
  retrofit_generator: any

flutter:
  uses-material-design: true
